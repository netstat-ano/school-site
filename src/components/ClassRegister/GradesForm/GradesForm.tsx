import StudentsList from "../StudentsList/StudentsList";
import StudentClass from "../../../models/StudentClass";
const GradesForm: React.FC<{
    selectedClass: StudentClass;
}> = (props) => {
    return (
        <div>
            <form></form>
        </div>
    );
};
export default GradesForm;
